type SelectorItem {
  id: ID! @unique
  ref: Selector!

  refersTo: RefersTo!

  # inherited from Entity
  hasProperty: [HasProperty!]!
  hasResolver: [HasResolver!]!

  createdAt: DateTime!
  updatedAt: DateTime!
}