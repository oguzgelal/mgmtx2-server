type LinkItem {
  id: ID! @unique
  ref: Link!

  hasSource: [HasSource!]!
  hasTarget: [HasTarget!]!

  # inherited from Entity
  hasProperty: [HasProperty!]!
  hasResolver: [HasResolver!]!

  createdAt: DateTime!
  updatedAt: DateTime!
}